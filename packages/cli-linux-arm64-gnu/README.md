# @intlify/cli-linux-arm64-gnu

This package is the optional target-specific native `intlify` binary package for `@intlify/cli` on Linux arm64 GNU.

Users normally install and invoke `@intlify/cli` rather than this package directly.
