# @intlify/cli-darwin-x64

This package is the optional target-specific native `intlify` binary package for `@intlify/cli` on macOS x64.

Users normally install and invoke `@intlify/cli` rather than this package directly.
