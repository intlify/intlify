# @intlify/cli-darwin-arm64

This package is the optional target-specific native `intlify` binary package for `@intlify/cli` on macOS arm64.

Users normally install and invoke `@intlify/cli` rather than this package directly.
