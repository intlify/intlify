# @intlify/cli-win32-x64-msvc

This package is the optional target-specific native `intlify` binary package for `@intlify/cli` on Windows x64 MSVC.

Users normally install and invoke `@intlify/cli` rather than this package directly.
