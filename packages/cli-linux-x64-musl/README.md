# @intlify/cli-linux-x64-musl

This package is the optional target-specific native `intlify` binary package for `@intlify/cli` on Linux x64 musl.

Users normally install and invoke `@intlify/cli` rather than this package directly.
